<?xml version="1.0" encoding="UTF-8" ?>
<!-- /mybatisstudy/src/mapper/MemberMapper1.xml -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="member"> <!-- 내부의 SQL 구문의 id 값 : namespace.id 속성 값으로 호출 -->
	<select id="count" resultType="int"> <!-- sql 구문 실행 후 전달되는 결과 레코드의 타입 -->
		select count(*) from member
	</select>
	<select id="list" resultType="Member">
		select * from member
	</select>
		<!-- 
		#{value} : 파라미터의 값 + 자료형.
				   value : ad 이라 가정
				   where id like %#{value}% → where id like %'ad'% → 오류 발생;
		${value} : 파라미터의 값의 치환
				   value : ad 이라 가정
				   where id lkie '%${value}%' → where id like '%ad%' → 정상 처리 
		-->
	<select id="selectid" resultType="Member" parameterType="String">
		select *
		from member 
		where id=#{value}
	</select>
	<select id="selectname" resultType="Member" parameterType="String">
		select *
		from member
		where name like '%${value}%'
	</select>
	<select id="selectid2" resultType="Member" parameterType="String">
		select *
		from member
		where id like #{value}
	</select>
	<select id="selectname2" resultType="Member" parameterType="Map">
		select * 
		from member
		where name like '%${name}%' and gender=#{gender}
	</select>
</mapper>